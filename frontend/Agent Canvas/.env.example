# Agent Reach — Frontend Environment (example)
# Copy this file to .env and adjust values.

# API mode: "http" (production) or "mock" (offline development)
VITE_API_MODE=http

# Backend base URL — where the FastAPI server is listening
VITE_API_BASE_URL=http://localhost:8000
